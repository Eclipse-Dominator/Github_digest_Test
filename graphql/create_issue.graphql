mutation ($owner: String!, $repo: String!, $title: String!, $body: String!) {
  createIssue(input: { repositoryName: $repo, repositoryOwner: $owner, title: $title, body: $body }) {
    issue {
      id
      url
    }
  }
}